/hive
.superpowers/
.worktrees/

# Claude eval session logs (any depth) — never commit
**/.claude/evals/sessions/

# Claude code-review scratch — machine-local
.claude/review/

# Claude worktree sessions — EnterWorktree puts a full checkout here, and an
# unignored one lands the whole tree back in the repo on any `git add -A`.
.claude/worktrees/
